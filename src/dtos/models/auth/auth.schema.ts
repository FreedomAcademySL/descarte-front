// import { object, string, InferType, boolean } from 'yup';
// import { MIN_LENGTH_PASSWORD } from '../../../constants/dto-constants';

// export const AuthSchema = object({
//   username: string().trim().required('username is a required field'),
//   password: string()
//     .trim()
//     .required('password is a required field')
//     .min(MIN_LENGTH_PASSWORD),
//   rememberMe: boolean(),
// });

// export type AuthSchemaType = InferType<typeof AuthSchema>;
